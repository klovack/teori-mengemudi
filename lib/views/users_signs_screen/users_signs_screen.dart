import 'package:flutter/material.dart';

class UsersSignsScreen extends StatelessWidget {
  const UsersSignsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Users Signs Screen'),
    );
  }
}
